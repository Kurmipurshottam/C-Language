#include<stdio.h>
int main()
{
	int length,width,area;
	printf("enter the length=");
	scanf("%d",&length);
	printf("enter the width=");
	scanf("%d",&width);
	area=length*width;
	printf("\narea of rectangle=%d",area);
	return 0;
}
